<div class="row justify-content-center">
  <div class="col center">
    <h2>Comment Edition</h2>
  </div>
</div>
<%= render 'layouts/errors', object: @comment, action: "saving changes" %>
<%= render 'comments/form', comment: @comment, verb:"save" %>
